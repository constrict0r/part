- *-x* (python executable): This parameter can only take the values *python*
  or *python3*, and indicates wich executable to use when running python tasks.

  If *python3* is available, this parameter defaults to *python3*, otherwise
  *python* is used.

 .. substitution-code-block:: bash

  ./|PROJECT|.sh -x python3